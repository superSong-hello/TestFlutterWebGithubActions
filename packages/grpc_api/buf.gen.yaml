version: v1
plugins:
  - plugin: buf.build/protocolbuffers/dart
    out: lib/
    opt:
      - grpc

# https://buf.build/docs/installation
# buf registry login
# buf generate buf.build/retail-ai/shinise-core --include-imports --include-wkt